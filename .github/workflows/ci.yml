name: CI

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: QuadriviumPress/bindery/.github/workflows/ci.yml@main
    with:
      install-hunspell: true
      pre-build-command: npm run verify
      build-command: npm run build
